apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "postgresql-openalt.fullname" . }}-test-connection"
  labels:
{{ include "postgresql-openalt.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "postgresql-openalt.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
