apiVersion: v1
kind: Service
metadata:
  name: {{ include "postgresql-openalt.fullname" . }}
  labels:
{{ include "postgresql-openalt.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "postgresql-openalt.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
